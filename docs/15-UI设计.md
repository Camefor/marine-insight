# UI 设计

## 1. 设计目标

目标用户经常在出发前或户外移动环境中快速查询。界面应安静、紧凑、适合扫描，优先回答“能不能去、主要风险是什么、什么时候去”，而不是把大量专业数字平铺给用户。

首屏直接进入可操作 Dashboard，不建设营销首页。桌面和移动端都要在首个视口显示地点查询、综合结论和最高风险。

## 2. 设计原则

- 结论优先：风险等级、主要原因和建议时间窗先于次要指标。
- 安全冗余：颜色、图标、文字和数值共同表达风险。
- 数据透明：来源、模型、更新时间、缓存状态和置信度可见。
- 活动导向：通过活动分段控件切换海钓、乘船、登岛、露营和摄影视角。
- 布局稳定：加载、切换小时和错误状态不能导致工具栏、指标格和图表跳动。
- 克制视觉：页面分区使用全宽带状或无框布局，卡片只用于独立指标/重复项，圆角不超过 8px，避免卡片嵌套。

## 3. 信息架构

```text
应用外壳
├── 海况 Dashboard（默认）
│   ├── 查询工具栏
│   ├── 综合结论与风险
│   ├── 活动评分
│   ├── 关键指标
│   ├── 逐小时趋势
│   └── 推荐窗口/小时详情
├── 地图选点
├── 收藏地点（登录）
├── 我的地点（登录）
├── 查询历史（登录）
├── 设置（登录）
└── 管理（管理员）
```

桌面端使用窄侧栏或顶部主导航；移动端使用底部导航承载 Dashboard、地图、收藏和设置，管理入口不占主导航。

## 4. Dashboard 布局

### 4.1 查询工具栏

- 地点搜索输入与候选列表。
- 地图图标按钮打开选点视图，使用 Lucide `MapPin`。
- 日期/时间选择器。
- `24h / 72h / 7d` 分段控件。
- 活动分段控件或紧凑菜单。
- 查询命令按钮带 `Search` 图标；刷新使用 `RefreshCw` 图标并提供 Tooltip。

移动端查询控件分两行或抽屉展开，最长地点名换行或省略并提供完整标题，不能覆盖按钮。

当前 `MI-0015` 基础 Dashboard 已先落地预置地点搜索、显式候选选择、UTC 起报时间、`24h / 72h / 7d` 范围和查询提交；`MI-0017` 已展示综合结论、活动评分和主要风险；`MI-0018` 已展示推荐时间窗、风险上升提示和保守返航截止；`MI-0024` 已补齐 Leaflet/OpenStreetMap 全宽地图选点、OSM 署名和经纬度输入降级。活动分段控件和刷新 Tooltip 留待后续阶段补齐。

### 4.2 综合结论区

- 地点名称是主标题，字号克制，不使用营销式超大标题。
- 显示分数、文字等级、风险图标和置信度。
- 展示 1 条结论、最多 3 条主要风险和推荐窗口/返航截止。
- 硬性风险使用固定警示带，禁止被折叠或移到屏幕下方。
- Weather、Marine、Tide 等来源分别显示 Provider、模型、发布时间、抓取时间和缓存状态；某一来源过期时不能被其他新鲜来源的状态遮盖。

### 4.3 活动评分

使用横向可滚动的活动标签或紧凑表格，显示活动图标、分数和文字等级。切换活动时复用同一页面结构，不通过弹窗展示主要结果。

### 4.4 关键指标网格

固定包含风速、阵风、有效波高、浪周期、涌浪、雷暴/对流和能见度。每项显示：

- 熟悉图标和指标名称。
- 当前值、单位和方向。
- 状态文字，如“平稳”“需关注”“高风险”“暂无数据”。
- 点击后打开无嵌套卡片的小时详情抽屉或侧面板。

指标容器使用稳定的最小高度和网格列，加载骨架与真实内容尺寸一致。

### 4.5 趋势与时间窗

- 图表默认显示风速/阵风、浪高/涌浪和综合分三个相关视图，使用 Tabs 切换，避免一次堆叠过多曲线。
- 风险阈值用水平参考线和文字标注，不只用背景颜色。
- Hover/触摸提示显示本地时间、全部可用值、来源和风险规则。
- 推荐窗口使用时间带展示；风险上升点和返航截止使用清晰图标与标签。

当前 `MI-0019` 已实现 Dashboard 内部的趋势 Tabs、推荐窗口时间带和小时详情面板：趋势可在分数、风、浪三组视图间切换，推荐窗口以横向时间带叠加，点击任一小时可查看完整指标、风险贡献和指标来源摘要。第三方 JS 图表库、复杂 Hover Tooltip 和独立抽屉动画仍待后续阶段。

## 5. 其他页面

| 页面 | 核心布局 | 主要状态 |
| --- | --- | --- |
| 地图选点 | 全宽地图 + 底部/侧边确认栏 | 定位拒绝、瓦片失败、选点中 |
| 收藏地点 | 可排序列表，显示最近结论摘要 | 空、加载、删除确认 |
| 我的地点 | 名称/经纬度/默认活动/备注表单 + 可排序列表 | 空、加载、保存校验、再次查询 |
| 查询历史 | 日期/地点筛选 + 紧凑表格/列表 | 空、分页、再次查询、删除确认 |
| 设置 | 单位分段控件、默认活动、时区选择 | 保存中、成功、验证失败 |
| 管理后台 | Tab 导航（运行状态 / 预置地点 / 用户）：运行状态 = Provider 状态表、算法版本 Tabs、审计表；预置地点 = 表格 + 新增/编辑表单 + 地图选点 + 删除确认（级联收藏提示）；用户 = 只读表格 | 未授权、加载、保存校验、删除确认 |

地图是实际选点工具，不放在装饰性卡片中。生产地图需显示合法署名。

## 6. 风险视觉规范

| 等级 | 建议颜色 | 图标/文字 |
| --- | --- | --- |
| VeryGood | 绿色 | `CircleCheck` + “非常适宜” |
| Good | 青绿色 | `Check` + “适宜” |
| Moderate | 黄色 | `Info` + “一般” |
| Caution | 橙色 | `TriangleAlert` + “谨慎” |
| Avoid | 红色 | `OctagonAlert` + “不建议” |
| Unknown | 中性灰 | `CircleHelp` + “数据不足” |

主界面使用中性背景、深色正文、绿色/黄色/橙色/红色风险语义和少量海蓝数据强调，避免整页由单一蓝色或深色调主导。

## 7. 组件视觉

- 图标优先使用 Lucide；图标按钮必须有可访问名称和 Tooltip。
- 二元设置使用 Toggle/Checkbox，模式使用 Segmented Control，数值使用输入/Stepper。
- 工具按钮使用熟悉图标，如刷新、收藏、定位、下载，不用带文字的圆角胶囊替代熟悉符号。
- 卡片圆角 <= 8px，阴影轻微；页面区段不做悬浮卡片。
- 字号不随视口连续缩放；使用固定层级和响应式布局换行。

## 8. 响应式设计

| 视口 | 布局 |
| --- | --- |
| 360-599px | 单列；查询两行/抽屉；指标 2 列；底部导航；图表可横向触摸 |
| 600-959px | 2 列主布局；指标 3 列；地图底部面板 |
| 960-1439px | 内容区 + 辅助风险栏；指标 4 列；完整趋势区 |
| >= 1440px | 限制内容最大宽度；增加信息密度，不放大字体 |

所有固定格式组件使用 `minmax`、`aspect-ratio` 或明确最小高度，避免状态变化引发布局位移。

## 9. 页面状态

- 初始：展示查询控件、常用地点和最近一次本地结果，不显示空白大卡片。
- 加载：保持旧结果可见并覆盖局部进度；首次加载使用同尺寸骨架。
- 空数据：明确缺少哪些关键指标，允许改时间/地点。
- 部分数据：可用指标正常展示，缺失项标记“暂无数据”，结论降低置信度。
- 过期缓存：顶部状态带列出具体过期的数据域、数据年龄和刷新操作。
- 错误：在结果区域显示可执行的重试，不只弹 Toast。
- 硬性风险：固定高可见警示，不允许被 AI 摘要弱化。

当前 `MI-0024` 阶段的初始状态展示查询控件、Leaflet/OpenStreetMap 地图选点、坐标输入降级和等待查询说明；查询成功后展示综合评分、活动评分、推荐时间窗、主要风险、趋势 Tabs、小时详情面板、来源、关键指标和逐小时评分表。收藏、潮汐、AI 和第三方 JS 图表仍待后续阶段补齐。

`MI-0026` 已在应用 Header 增加匿名登录/注册入口和登录后的邮箱/退出状态，并增加静态 SSR 登录、注册和无权访问页面。账户页面使用单一紧凑表单、明确错误状态和移动端单列布局；收藏、历史和设置导航仍待对应页面完成后加入。

`MI-0027` 已增加收藏、查询历史、单位设置和管理员运行状态页面。Dashboard 对登录用户加载风速、浪高、温度和默认活动偏好，换算只影响展示；收藏按默认活动再次查询，历史链接恢复地点、起报 UTC、范围和首个活动。360px/桌面 Playwright 布局用例已纳入仓库，但当前环境浏览器下载超时，视觉准出仍待可用浏览器环境执行。

`MI-0041` 按 `docs/持续迭代/2026-08-19-需求.md` 的设计图统一站点品牌为 `Marine AI`：Header 使用圆形海岛日落与海浪图标、深海蓝 `Marine` + 亮蓝 `AI` 字标，并保留「海岛海况智能决策平台」小字；首页 H1 使用「海岛海况智能决策平台」，紧随指定 Slogan「为海钓、露营、摄影和航海而生。」。品牌增强不改变首屏直接进入可操作 Dashboard 的产品原则，移动端隐藏 Header 小字以给主导航和账户入口保留空间。

`MI-0043` 依据 `docs/持续迭代/UI设计` 的完整原型与评审建议重构全站视觉：应用外壳切换为深海蓝暗色基底，以青色作为查询、推荐和可交互状态强调，以珊瑚橙/沙黄色表达风险与提醒；Dashboard 首屏采用「品牌结论 + 查询工作台」结构，查询仍以地点搜索为主，地图和坐标保持默认收起的辅助入口。结果区统一为半透明仪表面板，等宽字体用于评分、时间和指标读数，逐小时趋势提升为页面视觉重心；功能介绍、账户、用户工作区、地图和后台 Tab 共用同一设计语言。移动端保留 2 列指标网格和关键列逐小时表，并新增海况、地图、收藏、设置底部导航；所有视觉状态仍保留文字、数值和边框语义，不只依赖颜色。

`MI-0045` 重做起报日期时间控件：保留浏览器原生 `datetime-local` 的键盘、桌面弹层和移动端系统选择器，在深色输入框右侧增加统一的日历时钟 SVG 与不少于 44px 的选择热区；时间值采用等宽数字，显示时区改为独立青色徽标。控件显式声明暗色系统配色，移动端输入字号为 16px 以避免 iOS 自动缩放，并在单列查询布局中保持整行宽度。

`MI-0047` 优化移动端起报时间：输入声明 `zh-CN`，并在移动端用 `yyyy-MM-dd HH:mm` 的可视读数固定展示 24 小时制，原生输入仍负责系统选择器、键盘和提交；控件移动端横向扩展 8px，同时将输入边框、外层渐变边框和焦点阴影收敛为单一层级，避免嵌套边框产生视觉噪声。

`MI-0049` 起报时间提交前校验 UTC 整点边界：控件以 `step=3600` 引导整点选择，按当前显示时区提示应选择的本地分钟；若换算后的 UTC 不是整点，查询在页面内返回中文调整提示，不再展示缓存层英文异常。半小时/四分之一小时偏移时区按实际偏移分钟提示（例如 `30` 分）。

`MI-0050` 将起报时间改为原生日期输入与 `00:00`–`23:00` 小时下拉组合，页面不再提供分钟选择入口；日期或小时变化时都重新构造本地时间并强制分钟、秒为 `00`。UTC 整点校验继续作为异常时区和非 UI 调用的保护；对半小时偏移时区提示切换整小时偏移时区，避免控件无法选择所需分钟。

`MI-0053` 使用 Ant Design Blazor `TimePicker` 替换起报小时下拉：日期仍由原生日期输入负责，时间弹层使用中文 24 小时制并仅展示小时列，格式固定为 `HH:00`；保留深色主题、移动端 16px 输入和整行触控宽度，选择结果继续按显示时区写回本地墙上时间。

`MI-0051` 使用 `docs/持续迭代/marine_ai_logos` 的珊瑚日落、深海蓝岛屿和青绿海浪图稿重制 Header、favicon、Apple Touch、PWA 与社交分享共用品牌主图。移动底部导航、收藏、再次查询、编辑、删除、设置和关闭等操作统一使用同一套 Lucide 线性图标，保留文字、Tooltip 与可访问名称；收藏激活态以同一星形图标填充表达，不再混用系统字符与 Emoji。

## 10. 可访问性

- 正文与背景符合 WCAG AA 对比度。
- 键盘可操作查询、Tabs、时间选择和图表数据表替代视图。
- 动态结果更新使用适度 `aria-live`，避免每个逐小时点都播报。
- 图表提供摘要和可访问数据表；风险不只依赖颜色。
- 尊重 `prefers-reduced-motion`，不使用无业务意义动画。

## 11. 变更记录

| 版本 | 日期 | 变更说明 |
| --- | --- | --- |
| 1.0 | 2026-07-13 | 建立面向快速决策的 Dashboard、状态和响应式规范 |
| 1.1 | 2026-07-13 | 增加 Weather/Marine/Tide 多来源时效展示 |
| 1.2 | 2026-07-30 | 记录 `MI-0015` 基础 Dashboard 查询栏、来源状态、关键指标和 metrics-only 展示边界 |
| 1.3 | 2026-07-30 | 记录 `MI-0017` Dashboard 综合结论、活动评分、主要风险和逐小时评分表投影 |
| 1.4 | 2026-07-31 | 记录 `MI-0018` Dashboard 推荐时间窗、风险上升和返航截止展示 |
| 1.5 | 2026-07-31 | 记录 `MI-0019` Dashboard 趋势 Tabs、推荐时间带和小时详情面板 |
| 1.6 | 2026-07-31 | 记录 `MI-0024` Leaflet/OpenStreetMap 全宽地图选点、OSM 署名和坐标输入降级 |
| 1.7 | 2026-08-12 | 记录 `MI-0026` 账户 Header 状态与登录、注册、无权访问页面 |
| 1.8 | 2026-08-13 | 记录用户工作区、单位展示换算、再次查询上下文和管理员运行状态页面 |
| 1.9 | 2026-08-14 | 记录 `MI-0030` 地图选点由 OpenStreetMap 切换为天地图瓦片、Leaflet 自托管与天地图署名 |
| 2.0 | 2026-08-14 | 记录 `MI-0033` Dashboard 客户端时区识别与动态显示：时间展示统一本地化并标注「显示时区」，起报时间输入按显示时区解释，失败降级北京时间（UTC+8） |
| 2.1 | 2026-08-14 | 记录 `MI-0034` Dashboard 移动端响应式优化：手机端逐小时表格隐藏次要列、状态带纵向堆叠、触摸惯性与触控目标增强，桌面端布局不变 |
| 2.2 | 2026-08-14 | 记录 `MI-0035` 功能介绍页 `/about`（静态 SSR）：Hero、核心功能卡片网格、适用场景、可信理由列表与免责声明；`MainLayout` 头部新增「海况查询/功能介绍」主导航并适配移动端换行 |
| 2.3 | 2026-08-14 | 记录 `MI-0036` 「我的地点」页面（`/my-locations`，登录 + InteractiveServer）：顶部新增/编辑表单（名称、纬度、经度、默认活动、备注、排序）与可排序列表（编辑/删除/再次查询）；查询历史页面新增删除单条/清空按钮（JS `confirm` 二次确认）；登录/注册表单接入 SVG 验证码（`.account-captcha` 样式 + 「看不清？换一张」刷新） |
| 2.4 | 2026-08-17 | 记录 `MI-0038` 地图选点自定义命名与地图点收藏：Dashboard 地图选点区新增「地点名称（可选）」输入（留空回退「自定义坐标」），摘要标题改为显示自定义名称；选中地图坐标点时摘要栏显示收藏星标；Favorites 页地图点收藏展示名称与坐标，并以 `/?lat=&lon=&name=` 生成「再次查询」链接还原自定义名称 |
| 2.5 | 2026-08-18 | 记录 `MI-0039` 后台管理模块：新增共享 `AdminTabs` 导航（运行状态/预置地点/用户），「运行状态」页标题改为「运行状态」并纳入 Tab；新增预置地点管理页（表格 + 新增/编辑表单 + 天地图选点 + 删除确认显示级联收藏引用数）与只读用户列表页；`AccountNav` 管理入口改为后台管理（管理员可见） |
| 2.6 | 2026-08-19 | 记录 `MI-0040` 首页查询交互优化：点击「查找地点」命中预置地点则地图跟随显示标记，未命中提示走地图选点；**地图默认收起**（点击「地图选点」按钮才展开）；地图选点区缩放控件附近新增定位图标（十字准星，首次进入不请求定位权限，用户主动点击才 `getCurrentPosition`，成功后标点跟随显示，失败给可恢复提示）；「地点名称（可选）」输入框在选点后自动经天地图反查最近地名填充（查不到留空），并附提示「该名称仅作为地点备注，收藏地点时将按此名称显示」；预置地点后台新增「设为首页默认地点」选项，首页默认预选该地点 |
| 2.7 | 2026-08-19 | 记录 `MI-0040` 跟进：逆地理编码改用天地图服务端权限 Key（`Map:Tianditu:ServerKey`），选点后自动反查最近地名在线上生效；浏览器瓦片仍用浏览器端 Key |
| 2.8 | 2026-08-19 | 记录 `MI-0041` Marine AI 品牌落地：从需求设计图提取透明圆形品牌图标并派生 favicon/Apple Touch 图标；Header 使用图标、Marine AI 字标和平台副标题；Dashboard 首屏使用指定平台标题与 Slogan，移动端隐藏品牌副标题避免导航重叠 |
| 2.9 | 2026-08-19 | 记录 `MI-0043` UI 大改：全站采用深海暗色设计系统、青色交互和珊瑚风险语义；Dashboard 重组为决策工作台，逐小时趋势与数据可信度成为视觉核心；账户/介绍/用户工作区/后台同步换肤，并增加移动底部导航、暗色可访问性和 reduced-motion 处理 |
| 3.0 | 2026-08-19 | 记录 `MI-0045` 起报时间控件重构：深色原生日期时间输入增加时区徽标、日历时钟图标和桌面/移动触控热区，保持平台原生选择体验并避免 iOS 自动缩放 |
| 3.1 | 2026-08-20 | 记录 `MI-0051` 品牌与图标统一：采用持续迭代图稿的珊瑚日落/青绿海浪主标识，Header、favicon、Apple Touch、PWA、社交分享共用同源资产；可见操作图标统一为 Lucide 线性风格 |
