{$MARINE_INSIGHT_SITE_ADDRESS:http://localhost} {
    encode zstd gzip

    header {
        -Server
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "geolocation=(self)"
    }

    reverse_proxy web:8080
}
